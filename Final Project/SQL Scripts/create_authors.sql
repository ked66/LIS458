CREATE TABLE lis458olfa22_dillonk2_project.authors (
	author_id int NOT NULL AUTO_INCREMENT,
    author_name varchar(80),
    author_nationality varchar(40),
    author_birth varchar(30),
    author_death varchar(30),
    constraint primary key(author_id)
);